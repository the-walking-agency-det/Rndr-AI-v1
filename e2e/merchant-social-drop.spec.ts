import { test, expect } from '@playwright/test';

test('Merchant Social Drop Flow', async ({ page }) => {
  // 1. Navigate to Social Feed
  // 2. Click Post Input
  // 3. Click Product Picker
  // 4. Select Product
  // 5. Post
  // 6. Verify Post appears with embedded product
  test.skip('Requires authenticated session and backend');
});
