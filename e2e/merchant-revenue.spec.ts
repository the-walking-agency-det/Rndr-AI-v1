import { test, expect } from '@playwright/test';

test('Merchant Revenue Dashboard', async ({ page }) => {
  // 1. Navigate to Dashboard -> Revenue
  // 2. Verify Total Earnings
  // 3. Verify Breakdown
  // 4. Verify Chart renders
  test.skip('Requires authenticated session and backend');
});
